spring:
  jpa:
    properties:
      hibernate:
        connection:
          url: jdbc:postgresql://postgres:5432/gym_dev
  flyway:
    url: jdbc:postgresql://postgres:5432/gym_dev
jms:
  trainer-working-hours-queue: trainer.working.hours.queue.dev