spring:
  jpa:
    properties:
      hibernate:
        connection:
          url: jdbc:postgresql://gym_postgres:6432/gym_stg
  flyway:
    url: jdbc:postgresql://gym_postgres:6432/gym_stg

jms:
  trainer-working-hours-queue: trainer.working.hours.queue.stg